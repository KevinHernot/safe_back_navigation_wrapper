name: safe_back_navigation_wrapper
description: Router-agnostic safe back navigation helpers for Flutter apps.
version: 0.1.0
homepage: https://github.com/KevinHernot/safe_back_navigation_wrapper
repository: https://github.com/KevinHernot/safe_back_navigation_wrapper
issue_tracker: https://github.com/KevinHernot/safe_back_navigation_wrapper/issues

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
